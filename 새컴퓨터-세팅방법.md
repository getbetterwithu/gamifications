# 🖥️ 새 컴퓨터에서 Claude 설정 켜기 (완전판)

> **이 파일 하나만 보면 끝.** 다른 문서·노션·레포 안 봐도 됩니다.
> 빈 컴퓨터에서 막힐 수 있는 모든 경우를 아래에 다 적어놨습니다.
> **목표**: 새 컴퓨터에서 딱 한 번 세팅 → 그 뒤로는 영원히 자동 동기화.

---

## 💡 먼저 알아둘 것 (구조)

- **설정 본체는 `claude-config`라는 별도 레포**에 있습니다 (이 게임 레포가 아님).
- 새 컴퓨터에서 그걸 한 번 받아오면(clone), 설정·스크립트·CLI 버전이 자동으로 따라옵니다.
- 이 파일은 그 받는 방법을 적은 **안내문**입니다. (게임 레포에 둔 이유: 어디서든 GitHub 웹으로 바로 읽으라고)

### 🔑 기억할 것 딱 하나 — claude-config 레포 주소
```
git@github-getbetterwithu:getbetterwithu/claude-config.git
```
HTTPS로 받으려면 (SSH 키 없을 때):
```
https://github.com/getbetterwithu/claude-config.git
```

---

# 📋 전체 흐름 한눈에

```
[0] 사전 준비 확인 (Git? SSH 키?)  ──안 되면→ 아래 0단계 해결법
        │
        ▼
[1] claude-config 레포 받기 (clone)
        │
        ▼
[2] 더블클릭  ──안 되면→  직접 명령 입력
        │
        ▼
[3] 확인 → 끝. 이후 영원히 자동.
```

---

# 0단계 — 사전 준비 (평생 1회)

## (1) Git 있는지 확인
터미널(맥) / PowerShell(윈도우)에:
```bash
git --version
```
- 버전 나오면 OK.
- **"command not found" 나오면** → Git 설치:
  - **맥**: `xcode-select --install` 실행 (또는 https://git-scm.com 에서 받기)
  - **윈도우**: https://git-scm.com/download/win 에서 받아 설치 (Git Bash도 같이 깔림)

## (2) SSH 키 있는지 확인
```bash
ssh -T git@github-getbetterwithu
```
- "Hi getbetterwithu!" 비슷한 인사 나오면 OK → **1단계로**.
- **실패하거나 권한 거부면** → 두 가지 길:
  - **(쉬운 길) HTTPS로 받기**: SSH 설정 건너뛰고, 1단계에서 **HTTPS 주소**로 clone (GitHub 로그인/토큰 입력하면 됨).
  - **(원래 방식) SSH 키 새로 설정**: 아래 "부록 A. SSH 키 설정" 참고.

## (3) (윈도우만) 개발자 모드 ON
설정 → 개인정보 및 보안 → 개발자용 → **"개발자 모드" 켜기**
→ 이걸 켜야 링크(`mklink`)가 관리자 권한 없이 됩니다. (안 켜면 관리자 권한 필요)

---

# 1단계 — claude-config 레포 받기 (clone)

## 맥 / 리눅스 / Git Bash
```bash
mkdir -p ~/dotfiles
git clone git@github-getbetterwithu:getbetterwithu/claude-config.git ~/dotfiles/claude-config
```

## 윈도우 PowerShell
```powershell
mkdir "$HOME\dotfiles" -Force
git clone git@github-getbetterwithu:getbetterwithu/claude-config.git "$HOME\dotfiles\claude-config"
```

## ⚠️ clone이 실패하면
- **"Permission denied (publickey)"** → SSH 키 문제. HTTPS로 다시:
  ```bash
  git clone https://github.com/getbetterwithu/claude-config.git ~/dotfiles/claude-config
  ```
  (GitHub 사용자명 + 토큰 입력. 토큰은 github.com → Settings → Developer settings → Personal access tokens)
- **"already exists / not empty"** → 이미 받은 적 있음. clone 말고 최신화:
  ```bash
  git -C ~/dotfiles/claude-config pull
  ```
- **"could not resolve host" / 타임아웃** → 인터넷 연결 확인.

---

# 2단계 — 세팅 실행

## 🟢 방법 A — 더블클릭 (제일 쉬움)

1단계로 레포를 받았으면, `~/dotfiles/claude-config/` 폴더를 열어 더블클릭:

| 컴퓨터 | 더블클릭할 파일 |
|--------|----------------|
| **맥** | `Setup-새컴퓨터.command` |
| **윈도우** | `Setup-새컴퓨터-윈도우.bat` |

- **맥**: 더블클릭 → Terminal 열리며 자동 진행. "끝났습니다" 뜨면 완료.
  - **"확인되지 않은 개발자" 경고** → 파일 **우클릭 → 열기** → "열기" 클릭.
  - 그래도 막히면 → 시스템 설정 → 개인정보 보호 및 보안 → 아래쪽 "확인 없이 열기".
- **윈도우**: 더블클릭 → **관리자 권한 요청 창에서 "예"** → 자동 진행.
  - 권한 창이 안 뜨거나 링크 실패 → 아래 **방법 B**로.

## 🔧 방법 B — 직접 명령 입력 (더블클릭이 안 될 때)

### 맥 / 리눅스 / Git Bash — 이 한 줄이면 끝
```bash
~/dotfiles/claude-config/claude-sync pull
```
→ `settings.json`·`CLAUDE.md`·`sync-cli-version.sh`를 자동으로 링크합니다.
→ 안에 들어있는 `ensure_links`가 알아서 링크를 걸어줍니다.

### 윈도우 — 명령 프롬프트(cmd)를 **관리자 권한**으로 열고:
```cmd
mkdir "%USERPROFILE%\.claude\scripts" 2>nul
mklink "%USERPROFILE%\.claude\CLAUDE.md"   "%USERPROFILE%\dotfiles\claude-config\CLAUDE.md"
mklink "%USERPROFILE%\.claude\settings.json" "%USERPROFILE%\dotfiles\claude-config\settings.json"
mklink "%USERPROFILE%\.claude\scripts\sync-cli-version.sh" "%USERPROFILE%\dotfiles\claude-config\scripts\sync-cli-version.sh"
```
- **"이미 있습니다" 에러** → 기존 파일을 먼저 옮기고 다시:
  ```cmd
  move "%USERPROFILE%\.claude\settings.json" "%USERPROFILE%\.claude\settings.json.bak"
  ```
  그 뒤 위 mklink 다시 실행.
- **"권한이 없습니다"** → cmd를 **관리자 권한으로** 다시 열기 (또는 개발자 모드 ON).

---

# 3단계 — 확인

```bash
ls -la ~/.claude/settings.json
```
**윈도우 PowerShell:**
```powershell
Get-Item "$HOME\.claude\settings.json" | Select-Object LinkType, Target
```

→ 화살표(`->`) 또는 Target이 `claude-config`를 가리키면 **성공**.

---

# ✅ 세팅 후 — 자동으로 되는 것들

한 번 세팅하면, Claude Code를 열 때마다 자동으로:

1. **설정 동기화** — `settings.json` 항상 최신 (세션 시작 `claude-sync pull`)
2. **스크립트 동기화** — 레포 안 모든 파일
3. **링크 자가복구** — 링크가 깨져도 다음 세션에 자동 복원
4. **CLI 버전 동기화** — VSCode 확장 ↔ npm CLI 버전 어긋나면 자동으로 맞춤
   → "1MB 이미지인데 5MB 초과" 같은 버그 **원천 차단**
5. **변경 백업** — 세션 끝날 때 `claude-sync push`로 자동 커밋·푸시

한쪽에서 설정을 바꾸면 → push → 다른 컴퓨터에서 열 때 pull → **양쪽이 같아집니다.**

---

# 🔁 솔직한 한계 (딱 하나)

빈 컴퓨터가 스스로 내 설정을 알 방법은 없습니다.
그래서 컴퓨터당 **최초 1회**, 위 "1단계 clone"만은 사람이 해야 합니다.
**그 1회 이후로는 영원히 손댈 것 없습니다.**

---

# 📎 부록 A — SSH 키 새로 설정 (getbetterwithu 계정)

HTTPS로 받았으면 이 부록은 건너뛰어도 됩니다. SSH를 쓰고 싶을 때만.

### 1) 키 생성
```bash
ssh-keygen -t ed25519 -C "june_wook@snu.ms.kr" -f ~/.ssh/id_ed25519_getbetterwithu
```
(비밀번호는 그냥 엔터로 비워도 됨)

### 2) `~/.ssh/config`에 호스트 별칭 추가
파일 `~/.ssh/config`를 열어 아래 추가 (없으면 새로 만듦):
```
Host github-getbetterwithu
  HostName github.com
  User git
  IdentityFile ~/.ssh/id_ed25519_getbetterwithu
```

### 3) 공개키를 GitHub에 등록
```bash
cat ~/.ssh/id_ed25519_getbetterwithu.pub
```
출력된 내용 전체 복사 → github.com → Settings → SSH and GPG keys → New SSH key → 붙여넣기.

### 4) 연결 테스트
```bash
ssh -T git@github-getbetterwithu
```
"Hi getbetterwithu!" 나오면 성공 → 1단계 clone(SSH 주소) 진행.

---

# 📎 부록 B — 윈도우에서 bash가 없을 때 (CLI 버전 자동화)

세팅 후 CLI 버전 자동 맞추기(`sync-cli-version.sh`)는 bash로 돕니다.
윈도우에 **Git Bash**가 깔려 있으면 자동으로 됩니다.
없거나 안 되면, 가끔 수동으로 한 줄만 쳐주면 됩니다:
```cmd
npm install -g @anthropic-ai/claude-code@latest
```
(VSCode 확장과 npm CLI 버전이 어긋났을 때만 필요. 평소엔 안 해도 됨.)
